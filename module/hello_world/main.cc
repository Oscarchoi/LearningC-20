#include <iostream>
#include <vector>

import math;

int main() {
  std::cout << "add(2000, 200): " << add(2000, 20) << std::endl;
  
//  std::vector<int> my_vector{1, 2, 3, 4, 5, 6 };
  
//  std::cout << "productAll({1, 2, 3, 4, 5, 6,}): " << productAll(my_vector)
//	    << std::endl;
}
